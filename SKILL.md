---
name: sw-content-production
description: 将 SW（Skill&Will）的会议纪要、培训文字稿、访谈、学员反馈、课程材料或其他原始资料，按现行 SW 品牌与生产规范自动生成小红书图文和微信公众号图文，并按人工 MiniMax 语音交接流程半自动生成视频号短视频。用于 SW 员工在 WorkBuddy 中新建内容单元、提炼选题、生成工作坊教案红 VI 成品候选、制作跨平台验收页、接收语音后生成 Remotion 视频，或修改、归档和验收既有内容。强制执行 SW 官方机构叙述、禁用个人归属、来源追溯、公众号 1000～1500 字、实质内容图、人工确认和人工发布。
---

# SW 内容生产

## 目标

把一份有明确来源和使用边界的原始资料转成：

- 小红书：自动完成文案、AI 位图封面、文章流轮播、拼接长图和发布文案候选；
- 公众号：自动完成 1000～1500 字正文、实质内容图、工作坊教案红 HTML、长图和分享文案候选；
- 短视频：自动完成口播内容稿和唯一 MiniMax 机器稿，等待员工手动生成语音；收到语音后自动继续分镜、字幕、Remotion 画面、BGM、音效和成片候选；
- 验收：自动建立同页查看和复制载荷，但不登录或发布到任何平台。

“自动生成”不取消最终人工确认。始终执行：

```text
AI 生成 -> 人审确认 -> 人工发布
```

## 每次开始前

1. 将 WorkBuddy 工作空间设为员工选择的本地内容工作目录，不假设固定电脑或绝对路径。
2. 完整读取 [品牌与机构口径](references/brand.md)、[生产工作流](references/workflow.md) 和 [质量验收](references/quality.md)。
3. 按任务读取平台规范：
   - 小红书：[xiaohongshu.md](references/xiaohongshu.md)
   - 公众号：[wechat.md](references/wechat.md)
   - 短视频：[video.md](references/video.md)
4. 首次使用或更换电脑时读取 [deployment.md](references/deployment.md) 并运行环境检查。
5. 明确来源、公开范围、授权状态、核心选题和用户当前授权的动作；缺失时可继续做内部提炼，但不得把结果标成可发布。

## 不可妥协规则

1. **SW 官方机构身份**：所有对外候选均以 SW、Skill&Will、我们或无署名机构编辑口吻表达。禁止个人姓名、称谓、个人栏目或可识别变体出现在标题、正文、图片、口播、字幕、封面、作者栏、文件显示名和发布元数据中。内部来源追溯可以保留原始姓名。
2. **不虚构机构经历**：个人第一人称材料不能机械改成“我们”。只提炼为 SW 可承担的专业解释，或在授权和脱敏明确后改成匿名案例。
3. **来源可追溯**：区分来源事实、AI 推导、机构观点、外部事实和待专业审核项。保留原始材料，不覆盖原件。
4. **固定品牌视觉**：小红书和公众号默认使用“工作坊教案·红”，使用随 Skill 交付的官方 Logo；不得每篇自动生成三套风格，不得退回测试期视觉。
5. **一个选题一个内容单元**：使用 `output/NN-选题名称/`，第一层只能有 `小红书/`、`公众号/`、`短视频/`、`AIworkspace/`。
6. **过程与成品分离**：所有草稿、提示词、源码、素材处理、测试、审阅和历史版本放进 `AIworkspace/`。三个平台目录只放人工确认的当前成品。
7. **先归档再替换**：替换当前成品前，完整移入 `AIworkspace/90-archive/<平台>/<时间>-vNN/`，不覆盖历史。
8. **无默认外部副作用**：不自动发布、排期、登录平台、发送内容或调用未获授权的计费接口。图片生成仅在 WorkBuddy 已提供并获员工允许的生成能力内执行。
9. **短视频人工语音门**：没有用户确认的真实音频，不锁定最终分镜、字幕时间轴或成片。
10. **密钥隔离**：密钥只放在员工本机受控配置或 WorkBuddy 连接器中，不写入 Skill、内容单元、日志、验收页或压缩包。
11. **验收页状态一致**：只有小红书逐页图实际可见、尺寸和数量合格且正文非空时，才允许轮播和复制正文；缺失或解码失败时显示原因并禁用按钮，验收状态必须失败，禁止出现“图片空白但复制可用”。公众号同理。
12. **视频时间稳定**：画面切换由镜头边界决定，不由字幕切片决定。禁止在每条字幕首尾做整帧淡入淡出或重置缩放动画；默认不得出现单帧黑闪、白闪或三帧内连续强切。
13. **首帧就是封面**：第一镜必须从 0 秒开始且为 `cover`；独立封面必须由视频实际第一帧导出。封面使用 1080×1920 工作坊教案红母版，把标题和关键主体放进中央 3:4 安全核心区；不得直接拉伸小红书 3:4 成图、用正文页冒充封面或先出现黑帧再进入标题。
14. **机器稿必须完成语气转换**：`tts-input.txt` 不能只是把英文字母拆开。常规 45～90 秒稿必须具备开头直接、冲突加强、解释放稳、结尾减速的语气逻辑，并保留 3～6 个语义边界停顿；必须通过 `voiceover-qc.mjs` 才能交员工生成音频。

## 标准执行

### 1. 建立内容单元

把原始资料先保存为本地文件，然后运行：

```bash
node <SKILL_DIR>/scripts/init-content-unit.mjs \
  --workspace <工作空间> \
  --topic <简短选题名> \
  --source <原始资料文件>
```

`<SKILL_DIR>` 是本 `SKILL.md` 所在目录。脚本自动分配两位编号、建立四目录、复制官方 Logo 和生产母版，并登记原始资料哈希。不要把员工姓名、聊天 ID、电脑路径或历史试验编号写进公开成品。

### 2. 形成统一编辑判断

在 `AIworkspace/content-plan.md` 写清：

- 核心受众与真实问题；
- 反直觉但可被材料支撑的核心判断；
- 证据、来源片段与不可越过的边界；
- SW 能正式承担的机构观点；
- 三个平台各自的入口、推进和结尾动作；
- 案例、专业、品牌和发布待确认项。

不要把同一篇稿件机械缩成长中短三版。小红书负责场景与阅读推进，公众号负责中篇专业解释，视频号负责口头冲突与可听懂的行动结论。

### 3. 自动完成小红书和公众号候选

不中断地完成以下动作：

1. 生成共享封面提示词和专业解释图方案到 `AIworkspace/10-shared-assets/`。
2. 使用 WorkBuddy 可用的图片生成能力生成无文字、高分辨率位图封面；禁止用 CSS、SVG 或代码插画冒充 AI 生图封面。
3. 生成小红书 `content.json`、`caption.md`，运行 `scripts/render-xhs.mjs` 渲染逐页图、拼接长图并检查。
4. 生成公众号 `article.json` 和实质内容图，运行 `scripts/build-wechat.mjs` 生成正文、HTML、长图、分享文案并检查；固定保留原始品牌头图、两张尾图、品牌介绍和商标说明原文，这些固定品牌内容不计入 1000～1500 字正文。
5. 运行跨平台验收页构建脚本，让员工在一个页面里查看两平台候选并复制发布载荷。构建脚本必须验证小红书 3–10 张 1080×1440 PNG 全部可解码并实际显示、正文非空；否则仍生成明确缺失页，但禁用小红书切换和复制按钮并以非零状态停止，不能沿用旧验收页。

自动生成阶段允许 WorkBuddy 自行修正文案长度、分页、语义断行和版面溢出；不要为常规内容反复询问视觉偏好。只有来源、授权、专业立场或产品口径缺失到足以改变结论时才停止询问。

### 4. 生成短视频口播并停在语音门

在 `AIworkspace/40-video/current/` 生成：

- `voiceover-draft.md`：给员工审核的自然口播内容稿；
- `tts-input.txt`：员工复制到海外 MiniMax 网页的唯一机器稿；
- `tts-settings.md`：声音、模型、速度和试听记录空表。

先读取 [短视频规范](references/video.md)，把口播内容稿写成真实可讲的短句和对话节奏；员工确认后，再按四段语气曲线生成唯一机器稿。不能只执行 `MBTI → M B T I` 替换。运行：

```bash
node <SKILL_DIR>/scripts/voiceover-qc.mjs \
  --draft <内容单元>/AIworkspace/40-video/current/voiceover-draft.md \
  --tts <内容单元>/AIworkspace/40-video/current/tts-input.txt \
  --report <内容单元>/AIworkspace/40-video/current/voiceover-report.json
```

检查通过后，才提示员工手动生成音频并把原始文件放进 `AIworkspace/40-video/current/audio-inbox/`。此时停止视频画面生产，但小红书和公众号不受影响。

### 5. 收到音频后继续短视频

收到音频后：

1. 用 `ffprobe` 检查可解码、时长、声道、采样率、截断、重复、异常静音和异常人物声音；保留原文件和 SHA256。
2. 以实际音频为唯一时间轴生成视听脚本、分镜、动画描述和字幕时间点。
3. 先填写 `cover-brief.md`，优先复用小红书封面的主题钩子、无文字主视觉和工作坊教案红 VI，但重新编排为视频 9:16。`scenes.json` 第一镜必须 `start=0`、`visual=cover`，标题按语义拆成 1–3 行并保持中央安全区可读。
4. 使用 Remotion 或经确认的等价方案生成 1080×1920、30 fps 竖屏视频；画面保留 SW 上下品牌区和中部有效内容区。常规镜头至少 1.8 秒，字幕至少停留 1.2 秒；音频空隙保持前一镜头，不得错误跳到其他镜头。
5. 镜头切换只发生在 `scenes.json` 的镜头边界；字幕变化只更新字幕层。默认使用稳定直切；确需转场时只在镜头边界使用 6–12 帧的单次过渡，不得在字幕边界给整帧做淡入淡出或让缩放动画归零。
6. 融合人声、可感知但不压人声的 BGM 和必要音效；执行媒体探测、抽帧和时间稳定检查。运行 `scripts/video-qc.mjs`，要求整帧黑闪为 0、三帧内往返强切为 0；同时要求 `video-cover.png` 为 1080×1920，和解码后第一帧 SSIM 不低于 0.98。未通过不得进入候选成片。
7. 把候选成片、`video-cover.png`、字幕和最终口播稿留在 `AIworkspace/40-video/current/output/` 等待确认。

### 6. 人工确认与成品提升

员工明确说“确认这版”后，才运行：

```bash
node <SKILL_DIR>/scripts/promote.mjs --unit <内容单元路径> --platform xhs,wechat --confirm YES
```

短视频确认后再单独加入 `video`。脚本会先归档旧成品，再复制当前候选。任何平台候选改变后，重新构建验收页；提升后使用 `--source final` 从当前成品重建一次。

### 7. 验收与回报

运行：

```bash
node <SKILL_DIR>/scripts/validate-content-unit.mjs --unit <内容单元路径>
node <SKILL_DIR>/scripts/build-final-review.mjs --unit <内容单元路径>
```

只回报证据真实达到的层级：静态检查、本地渲染、人工内容确认、专业审核、素材授权、品牌确认、发布授权。不得把“文件已生成”写成“可以发布”，也不得把“人工发布准备完成”写成“已发布”。

## 停止条件

遇到以下情况停止进入成品或发布流程，并准确说明需要哪位员工确认：

- 原始资料没有来源、上下文或公开范围；
- 学员、客户、企业或合作方案例授权不清；
- MBTI、认证、课程、心理健康或机构立场存在专业争议；
- 对外候选命中禁用个人归属或把个人经历虚构成机构经历；
- 需要生成计费图片、使用受限素材、登录或发布，但没有明确授权；
- 公众号少于 1000 字、超过 1500 字、材料不足却以空话凑字数，或没有实质内容图；
- 小红书封面不是 AI 位图/获授权真实素材，或正文退化成重复卡片/PPT；
- 短视频尚未收到确认音频，或音频质量不足以建立时间轴；
- 口播稿仍像书面文章、机器稿未完成语气转换、停顿为 0 或过密，或 `voiceover-qc.mjs` 未通过；
- 最终验收页的平台画面与复制按钮状态不一致，或仍在引用旧载荷；
- 视频出现非脚本化整帧黑闪、三帧内往返强切、字幕边界整帧淡入淡出或动画重置；
- 视频第一镜不是封面、独立封面与第一帧不一致、标题/关键主体离开中央安全区，或封面仍是被拉伸的小红书成图；
- 实际准备发布的版本与人工确认版本不一致。

## 交付回报格式

用七项紧凑回报：

1. 内容单元与原始来源；
2. 小红书候选及页数、尺寸、长图、文案；
3. 公众号候选及正文计数、实质配图、HTML、长图；
4. 短视频当前停在口播确认、等待音频或成片候选的哪一阶段；
5. 技术检查与实际证据；
6. 专业、授权、品牌和发布状态；
7. 员工下一步只需完成的人工动作。
